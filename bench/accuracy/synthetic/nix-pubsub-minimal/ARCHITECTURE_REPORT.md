# private-tmp-repo-audit-code-graph-bench-accuracy-synthetic-nix-pubsub-minimal

_Architecture orientation report, auto-generated by `code-graph` from the indexed graph. Regenerated on every `index_repository`; regenerate manually with the `generate_report` MCP tool._

- **Graph**: 31 nodes, 29 edges

## Start Here

If you are a coding assistant orienting to this repo, read this report **before** running `Glob`/`Grep` on the files. It tells you the high-degree functions (god nodes), the natural communities of code, and the cross-package boundaries. Drill in with `query_graph`, `trace_call_path`, or `get_relevant_context` from the code-graph MCP.

## Suggested Questions

The graph is uniquely positioned to answer these. Prefer `query_graph` / `trace_call_path` / `get_relevant_context` over raw file search.

1. **What are the dead-code candidates?** — `search_graph(label="Function", relationship="CALLS", direction="inbound", max_degree=0, exclude_entry_points=true)`
2. **What is the blast radius of changing a file?** — `detect_changes(scope="file", path="<path>")`

---

_Report refreshes on every `index_repository` run. For manual regeneration: call the `generate_report` MCP tool. This file is safe to commit to git so teammates get an instant orientation after `git pull`._
